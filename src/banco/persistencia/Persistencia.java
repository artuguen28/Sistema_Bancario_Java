package banco.persistencia;

import banco.negocio.ContaCorrente;

public class Persistencia {

	/* Método ilustrativo para salvar o estado atual dos objetos de ContaCorrente.
	* Em caso de uso de banco de dados (Oracle, MySQL, etc.) para persistência de dados, 
	esta seria a classe responsável pela ponte/interação entre o programa e o banco de dados. */
	
	public boolean salvar(ContaCorrente conta)
	{
		return true;
	}

}
